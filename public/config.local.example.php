<?php
/**
 * ШАБЛОН конфигурации формы заявок.
 *
 * ЧТО СДЕЛАТЬ НА СЕРВЕРЕ (reg.ru, в менеджере файлов ISPmanager):
 *   1. Скопируй этот файл рядом и назови ровно: config.local.php
 *   2. Впиши реальные данные ниже.
 *   3. Этот пример (config.local.example.php) можно оставить — в нём нет секретов.
 *
 * ВАЖНО (152-ФЗ): ящик-получатель 'to' должен быть РОССИЙСКИМ
 * (Яндекс 360 / reg.ru / Mail.ru). НЕ Gmail и не другой зарубежный сервис —
 * иначе персональные данные снова уходят за границу.
 *
 * Пароль SMTP — это «пароль приложения», а НЕ основной пароль от почты.
 * Создаётся в настройках почтового ящика (Яндекс/Mail.ru — раздел «Пароли приложений»).
 */

return [
    // --- SMTP отправителя (implicit SSL, порт 465) ---
    // Примеры хостов:
    //   reg.ru почта:  уточни в ISPmanager → Почта (обычно вида mail.hosting.reg.ru)
    //   Яндекс 360:    smtp.yandex.ru
    //   Mail.ru:       smtp.mail.ru
    'smtp_host' => 'smtp.yandex.ru',
    'smtp_port' => 465,
    'smtp_user' => 'zayavka@info-forvard.ru',      // логин ящика-отправителя
    'smtp_pass' => 'ВПИШИ_ПАРОЛЬ_ПРИЛОЖЕНИЯ',       // пароль приложения (не основной!)

    // --- от кого уходит письмо (обычно совпадает с smtp_user) ---
    'from'      => 'zayavka@info-forvard.ru',
    'from_name' => 'Сайт ИНФО-ФОРВАРД',

    // --- куда падают заявки (РФ-ящик; можно несколько через запятую) ---
    'to'        => 'info@info-forvard.ru',
];
