# Превью-копия сайта на GitHub Pages — для согласования до заливки на reg.ru.
# Боевой сайт живёт на reg.ru, эта копия закрыта от индексации.
name: Preview to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run build
        env:
          NEXT_PUBLIC_BASE_PATH: /info-forvard-site
      - name: Закрыть превью от поисковиков
        run: printf 'User-agent: *\nDisallow: /\n' > out/robots.txt
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
